#include <stdio.h>

int main() {
    int a, b, c;

    printf("Entre com valores de a, b, c:")
    scanf("%d %d %d", &a, &b, &c);
    if( a>b && a>c) {
        printf("a é o maior\n");
    }
    else if ( b>c ) {
             printf("b é maior\n");
    }
    else {
      printf"c é maior\n")
    }


            